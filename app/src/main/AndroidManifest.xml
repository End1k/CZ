<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/loadinga"
        android:label="@string/Krenol"
        android:roundIcon="@mipmap/loadinga"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Avtor"></activity>
        <activity android:name=".AfterGameActivity" />
        <activity android:name=".vsRealPlayerActivity" />
        <activity android:name=".vsRealPlayerActivityEnternet" />
        <activity android:name=".Mainest2Activity" />
        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>